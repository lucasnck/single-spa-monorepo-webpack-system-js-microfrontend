export interface CliOptions {
  projectName: string;
  projectPort: number;
  templateName: string;
  templatePath: string;
  tartgetPath: string;
}

export interface TemplateData {
  projectName: string;
  projectPort: number;
}
